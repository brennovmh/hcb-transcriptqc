# Thresholds

Thresholds are stored in `conf/thresholds.yaml` and separated into:

- `transcriptome`
- `panel_hybrid_capture`
- `panel_amplicon`

These are initial operational defaults, not universal clinical cutoffs.
